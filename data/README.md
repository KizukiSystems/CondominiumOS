# data/ — LANE B. Everything here is gitignored, and that is the point.

This directory is the only sanctioned place for real client material (Corporation 94, Isaac's exports) once Lane B opens, and Lane B is CLOSED until Build Spec §6 (data handling) is ratified.

Rules, from CLAUDE.md:

- Nothing under `data/` is ever committed. The repo's `.gitignore` enforces this; this README is the single committed exception.
- Real and fictional data never mix. Fictional demo material (Maple Court / OCSCC 742) lives in `fixtures/`, committed. Real material, when permitted at all, lives here, uncommitted.
- If a task is ambiguous about which lane it is in, it is Lane B: stop and flag.
