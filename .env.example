# Copy to .env (gitignored) and fill in. Keys never enter the repo.
# All LLM access goes through src/llm/adapter.ts (queued: pre-build item 2).
# Default provider: Gemini. Drop-in alternative: Anthropic.
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
