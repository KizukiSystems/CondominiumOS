# src/classify/ — sort the corpus

Arrives with the M1 pipeline (WP-1.3): separates decision-relevant threads from routine noise before generation. The fixture corpus deliberately contains routine distractors so this stage is testable.
