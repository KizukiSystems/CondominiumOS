# src/agenda/ — generate the pack

Arrives with the M1 pipeline (WP-1.3), after the gate locks the wedge; this is the one wedge-named directory and renames freely if Friday names a different workflow. Hard rules from Build Spec §2: no agenda item, background sentence, or option without at least one citation; generation that cannot cite emits `[uncited — needs source]` instead of prose; the Manager's Suggestion comes from manager input, never from the model (SALVAGE.md §4 repair rules, ratified 2026-07-23).
